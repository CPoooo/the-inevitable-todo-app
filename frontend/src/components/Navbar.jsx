function Navbar() {

  return (
    <div>
      <nav>
        <h1 className="navHeader">todoApp</h1>
        <button className="navButton">Sign In</button>
      </nav>
    </div>
  );
}

export default Navbar;
