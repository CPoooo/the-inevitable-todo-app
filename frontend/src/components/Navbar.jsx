function Navbar() {

  return (
    <div>
      <nav>
        <h2 className="navHeader">todoApp</h2>
        <button className="navButton">Sign In</button>
      </nav>
    </div>
  );
}

export default Navbar;
